apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-comm-ing
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: demo-comm-svc
                port:
                  number: 8080
