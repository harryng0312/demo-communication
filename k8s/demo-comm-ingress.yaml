apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-comm-ing
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: harryng-mbp
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-comm-svc
                port:
                  number: 8080
